Security Scanning (ClamAV) — Project Status

As of 2026-05-18, this repository contains no ClamAV integration or file-scanning logic.
Issue #18 requests optimizing ClamAV scanning logic, but no such logic exists here.
When adding scanning later, prefer clamd INSTREAM, add timeouts, size limits, and cache-clean checksums.
