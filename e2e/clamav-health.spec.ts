\nimport { test, expect } from '@playwright/test';\n\ntest('clamav health endpoint exposes enabled flag', async ({ page }) => {\n  const resp = await page.goto('/api/clamav/health');\n  expect(resp, 'response should be defined').toBeTruthy();\n  expect(resp!.ok(), 'HTTP status should be OK').toBeTruthy();\n  const body = await page.evaluate(() => document.body.innerText);\n  const json = JSON.parse(body || '{}');\n  expect(typeof json.enabled).toBe('boolean');\n});\n